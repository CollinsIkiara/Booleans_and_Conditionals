The above Python projects I did demonstrate implementation of concepts in **Boolean Logic and Conditional Statements**, specifically on the following areas:

1. An understanding of how to work with the boolean data types - True & False.
2. An understanding of how conditional statements (i.e. _if_, _elif_, _else_) and Boolean/logical operators (i.e. <, >, <=, >=, ==, !=) work.
3. What _truthy_ and _falsy_ values are and how Boolean operators and **_Short-Circuiting_** work together.

All you need to do is run the file on a terminal to get the output.
